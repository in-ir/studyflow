# StudyFlow backend dependencies
# Install with:  pip install -r requirements.txt

# --- Web framework ---
fastapi==0.141.1
uvicorn[standard]==0.52.4      # ASGI server; [standard] adds uvloop/httptools/websockets
python-multipart==0.0.32       # required by FastAPI for File()/Form() uploads

# --- Config & validation ---
python-dotenv==1.2.3
pydantic[email]==2.13.5        # [email] pulls in email-validator, needed by EmailStr

# --- Auth ---
PyJWT==2.13.0
bcrypt==5.0.0

# --- AI assistant ---
anthropic==1.3.0

# --- Document parsing for chat uploads ---
pypdf==6.16.2                  # replaces the unmaintained PyPDF2
python-docx==1.2.0

# --- Course catalog scraper (fast_scraper.py) ---
requests==2.34.2
beautifulsoup4==4.15.0
